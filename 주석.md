# [공식] 식약처 스튜디오 코드 주석 작성 표준 가이드 (주석.md)

---

## 1. 개요 및 대원칙

본 문서는 `C:\식약처\work` 저장소 내 모든 **Python, TypeScript, TSX** 코드에 적용되는 공식 코드 주석 및 도큐멘테이션 표준이다.

### 5대 기본 원칙
1. **문체 통일 (개조식 종결어미)**: 모든 설명문은 **`~함`**, **`~임`**, **`~음`** 형태의 명사형/단답식 종결어미를 사용한다. (`~합니다`, `~이다` 서술문 금지)
2. **이모지(Emoji) 사용 전면 금지**: 유니코드 이모지(예: 아이콘, 표정, 특수 심볼)는 파일 인코딩 깨짐 방지 및 공공 표준 품질 유지를 위해 주석 내 일체 사용을 금지한다.
3. **파일 헤더 의무화**: 직접 작성한 모든 소스 파일 최상단에 표준 파일 헤더 블록을 작성한다.
4. **일반 함수(Function)**: `//` (Python은 `#`) 한 줄 인라인 주석으로 간결하게 목적과 역할을 기술한다.
5. **클래스 메서드(Method)**: `@` 기호 기반 태그(`@description`, `@param`, `@returns`, `@throws`) JSDoc/Docstring 형식을 엄격히 적용한다.

---

## 2. 파일 헤더 표준 (File Header Docs)

모든 파일의 첫 줄에 배치하며, 개인 PC의 로컬 절대경로(`C:\Users...`) 대신 **저장소 상대 경로**를 사용한다.

### 2.1 Python 파일 헤더 (`#` 박스 스타일)
```python
# -*- coding: utf-8 -*-
# =============================================================================
# 파일명: ocr_table_reconstructor.py
# 경로: packages/synthetic_engine/synthetic_engine/exporters/ocr_table_reconstructor.py
# 목적: 스캔본·디지털 PDF의 OCR, 표(Table) 격자 복원 및 스타일 보존을 수행함
# 작성자: 개발팀
# 작성일: 2026-09-09
# 수정일: 2026-09-10
# =============================================================================
from __future__ import annotations
```

### 2.2 TypeScript / TSX 파일 헤더 (`/** ... */` 블록 스타일)
```typescript
/**
 * 파일명: DataConverterStudio.tsx
 * 경로: apps/web/src/features/converter/DataConverterStudio.tsx
 * 목적: 데이터·문서 변환 작업 화면 및 미리보기를 제공함
 * 작성자: 개발팀
 * 작성일: 2026-09-09
 * 수정일: 2026-09-10
 */
import React, { useState, useEffect } from 'react';
```

---

## 3. 일반 함수(Function) 주석 표준

- 독립 함수, 유틸리티 함수, 람다, 화살표 함수(Arrow function)에 적용.
- 불필요한 장황한 설명 없이 **`//` (Python은 `#`)**을 사용해 무엇을 하는지 **`~함` / `~임`**으로 간결히 명시.
- 이모지를 일체 포함하지 않는다.

### 3.1 TypeScript / TSX 일반 함수
```typescript
// 입력 파일의 확장자를 정규화된 소문자로 추출함
export const extractExtension = (fileName: string): string => {
  return fileName.slice(fileName.lastIndexOf('.')).toLowerCase();
};

// 업로드된 데이터셋의 행 수가 최소 요구치(50행)를 만족하는지 검증함
export function validateRowCount(rows: unknown[]): boolean {
  // 빈 배열이거나 데이터가 없는 비정상 상태임
  if (!rows || rows.length === 0) return false;
  return rows.length >= 50;
}
```

### 3.2 Python 일반 함수
```python
# 이미지의 밝기 분포를 계산하여 어두운 배경인지 판별함
def is_dark_image(img_gray: np.ndarray, threshold: float = 127.0) -> bool:
    # 픽셀 평균값이 임계값 미만인 경우 어두운 이미지로 판정함
    mean_val = float(np.mean(img_gray))
    return mean_val < threshold

# 주민등록번호 문자열의 7번째 자리를 마스킹하여 반환함
def mask_resident_id(raw_id: str) -> str:
    # 정규식 패턴과 일치하지 않는 유효하지 않은 입력값임
    if not re.match(r'^\d{6}-[1-4]\d{6}$', raw_id):
        return raw_id
    return raw_id[:8] + '******'
```

---

## 4. 클래스 메서드(Method) 주석 표준 (`@` 태그 스타일)

클래스 내부의 인스턴스 메서드, 정적 메서드(`static`), 비동기 메서드는 IDE 툴팁 지원과 자동 문서화를 위해 **`@` 태그 기반 블록 주석**을 필수로 작성한다.

### 4.1 TypeScript 클래스 메서드 (JSDoc `@` 태그)
```typescript
export class DocumentConversionService {
  /**
   * @description 업로드된 스캔 문서를 원본 서식 1:1 HWPX 파일로 변환함
   * @param {File} sourceFile - 변환 대상 원본 이미지 또는 PDF 파일 객체임
   * @param {ConversionOptions} options - 표 복원 및 OCR 정밀도 설정 옵션임
   * @returns {Promise<ConversionResult>} 변환된 파일 URL 및 무결성 감사 리포트를 반환함
   * @throws {UnsupportedFileError} 지원하지 않는 파일 포맷인 경우 발생함
   * @throws {OcrProcessingError} OCR 판독 엔진 실패 시 발생함
   * @note 대용량 PDF(50페이지 이상)는 비동기 작업 큐로 위임 처리함
   */
  public async convertToHwpx(
    sourceFile: File,
    options: ConversionOptions
  ): Promise<ConversionResult> {
    // 내부 비즈니스 로직 실행...
  }

  /**
   * @description 표 셀의 RGB 배경색을 HEX 컬러 코드로 정규화함
   * @param {number[]} rgb - [R, G, B] 정수 배열(0~255)임
   * @returns {string} #RRGGBB 형태의 헥사 코드 문자열임
   */
  private normalizeCellColor(rgb: number[]): string {
    return `#${rgb.map(v => v.toString(16).padStart(2, '0')).join('')}`;
  }
}
```

### 4.2 Python 클래스 메서드 (`@` 명시형 Docstring)
```python
class TableGridReconstructor:
    """문서 내 표 격자선 및 병합 셀 구조를 복원하는 엔진 클래스임"""

    def __init__(self, dpi: int = 300):
        self.dpi = dpi

    def detect_table_cells(
        self,
        img_gray: np.ndarray,
        min_cell_area: int = 400
    ) -> list[GridCell]:
        """
        @description 모폴로지 연산과 투영 프로파일을 결합하여 표 셀 격자를 분할 추출함
        @param img_gray: 전처리 완료된 그레이스케일 이미지 배열임
        @param min_cell_area: 노이즈 필터링을 위한 최소 셀 면적 픽셀 단위임
        @return: 추출된 GridCell 객체 리스트(row_span, col_span 포함)를 반환함
        @throws ValueError: 입력 이미지가 비어 있거나 유효하지 않은 차원인 경우 발생함
        @note 무괘선 표(Borderless)의 경우 X/Y 투영 히스토그램으로 대체 추론함
        """
        if img_gray is None or img_gray.size == 0:
            raise ValueError('유효하지 않은 빈 이미지 데이터임')

        # 셀 분할 알고리즘 수행...
        return []
```

---

## 5. 인라인 주석 및 특수 관리 태그 표준

코드 내부 로직 중 설계 의도나 주의사항을 표기할 때는 허용된 특수 태그를 대문자로 작성하고, 반드시 **`~함` / `~임`** 종결어미를 붙인다. 이모지는 금지한다.

| 태그 | 용도 | 작성 예시 |
|---|---|---|
| **`NOTE`** | 비직관적인 설계 이유 및 배경 설명 | `// NOTE: 한글 서식의 이중선 테두리는 HWPX 사양상 0.5mm 간격으로 보정함` |
| **`WARN`** | 부작용(Side-effect)이나 주의사항 | `# WARN: 10,000건 초과 데이터는 메모리 누수를 방지하기 위해 청크 단위로 처리함` |
| **`TODO`** | 향후 개선할 백로그 및 과제 | `// TODO: 고해상도 이미지(4K 이상) 타일 분할 추론 알고리즘 적용 예정임` |
| **`FIXME`** | 임시 땜질 코드 또는 알려진 버그 | `# FIXME: OpenCV 4.9.0에서 특정 PNG 알파 채널 깨짐 현상 임시 우회 처리임` |
| **`SECURITY`** | 보안, 개인정보 가명화, 인젝션 방어 | `// SECURITY: 클라이언트 입력 HTML 태그는 XSS 방지를 위해 DOMPurify로 소독함` |

---

## 6. 금지 및 권장 사항 요약

### [금지] 절대 금지 패턴 (Anti-Patterns)
1. **이모지(Emoji) 사용**:
   - 금지: `// [불가] 힌트: 캐시를 갱신함`, `# [불가] 주의: 메모리 누수 발생`
   - 준수: `// NOTE: 캐시를 갱신함`, `# WARN: 메모리 누수 방지를 위해 해제함`
2. **서술형 종결어미 사용**:
   - 금지: `// 이 함수는 사용자의 입력을 검증합니다.`
   - 준수: `// 사용자 입력값을 검증함`
3. **코드 단순 번역 주석**:
   - 금지: `i = i + 1  # i에 1을 더한다`
   - 준수: 주석 불필요 (자명한 코드는 코드 자체로 설명)
4. **개인 로컬 절대경로 노출**:
   - 금지: `경로: C:\Users\Administrator\Desktop\work\engine.py`
   - 준수: `경로: packages/synthetic_engine/synthetic_engine/engine.py`
5. **죽은 코드 주석 처리(Zombie Code)**:
   - 금지: 수십 줄의 구버전 코드를 주석으로 방치
   - 준수: Git 히스토리로 관리하고 과감히 삭제

### [권장] 적극 권장 패턴 (Best Practices)
1. **함수는 간결하게 `//` 한 줄**, **메서드는 풍부하게 `@` 태그**.
2. **예외 처리 시 이유 명시**:
   ```python
   except FileNotFoundError:
       # 설정 파일 부재 시 시스템 기본 프리셋으로 안전하게 폴백함
       return load_default_preset()
   ```
3. **정규식 및 매직 넘버 설명**:
   ```typescript
   // 식약처 품목기준코드 9자리(숫자) 패턴 정규식임
   const ITEM_CODE_REGEX = /^\d{9}$/;
   ```