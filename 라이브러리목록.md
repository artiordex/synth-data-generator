# 프로젝트 사용 라이브러리 및 오픈소스 목록

> **자동 동기화 일시**: `2026-09-09 21:00:46`
> 이 문서는 `package.json`, `package-lock.json`, `pyproject.toml`의 의존성을 기반으로 자동 갱신됩니다. 패키지를 추가하면 다음 동기화 시 설명과 버전이 반영됩니다.

## 1. 웹 프론트엔드 핵심 UI (`apps/web` - package.json)
- **`@tanstack/react-virtual`** (`^3.13.2`) : 대용량 표 데이터 가상화 스크롤 (화면 버벅임 없는 대규모 행 렌더링)
- **`clsx`** (`^2.1.1`) : 조건부 CSS 클래스 네임 문자열 결합 유틸리티
- **`dompurify`** (`^3.2.4`) : HTML/마크다운 렌더링 XSS 보안 정제 및 악성 스크립트 실행 방지 (공공기관 보안 필수)
- **`file-saver`** (`^2.0.5`) : 브라우저 클라이언트 사이드 즉시 파일 다운로드 트리거
- **`jszip`** (`^3.10.1`) : 다중 파일 및 대량 변환 결과물 브라우저 단 ZIP 압축 아카이브 생성
- **`lucide-react`** (`^0.395.0`) : 고품질 커스텀 벡터 아이콘 셋 (UI 버튼, 인디케이터, 메뉴)
- **`marked`** (`^18.0.12`) : 초고속 GFM(GitHub Flavored Markdown) 및 표준 마크다운 파서·컴파일러
- **`papaparse`** (`^5.5.2`) : 초고속 브라우저 CSV/TSV 파서 및 파일 인코딩(EUC-KR, UTF-8) 자동 감지
- **`react`** (`^18.3.1`) : 선언적 컴포넌트 기반 웹 사용자 인터페이스 라이브러리
- **`react-dom`** (`^18.3.1`) : React 가상 DOM의 브라우저 DOM 렌더링 엔진
- **`recharts`** (`^2.15.1`) : 원본 vs 합성 데이터 분포 비교 및 다변량 통계 상관관계 인터랙티브 차트
- **`tailwind-merge`** (`^2.3.0`) : Tailwind CSS 유틸리티 클래스 충돌 방지 및 안전한 병합

## 2. 웹 빌드 툴 & 컴파일러 (`devDependencies`)
- **`@rollup/rollup-win32-x64-msvc`** (`^4.63.1`) : NPM 개발 및 빌드 의존성
- **`@types/dompurify`** (`^3.2.0`) : DOMPurify TypeScript 타입 정의
- **`@types/file-saver`** (`^2.0.7`) : FileSaver TypeScript 타입 정의
- **`@types/jszip`** (`^3.4.1`) : JSZip TypeScript 타입 정의
- **`@types/papaparse`** (`^5.3.15`) : PapaParse TypeScript 타입 정의
- **`@types/react`** (`^18.3.3`) : React 코어에 대한 TypeScript 타입 정의
- **`@types/react-dom`** (`^18.3.0`) : React DOM에 대한 TypeScript 타입 정의
- **`@vitejs/plugin-react`** (`^4.3.0`) : Vite용 공식 React Fast Refresh 빌드 플러그인
- **`autoprefixer`** (`^10.4.19`) : 브라우저 호환성을 위한 CSS 벤더 프리픽스 자동 추가 도구
- **`postcss`** (`^8.4.38`) : CSS 변환 및 최적화 파이프라인 엔진
- **`tailwindcss`** (`^3.4.4`) : 유틸리티 퍼스트 CSS 프레임워크 (디자인 토큰, 다크모드, 반응형 레이아웃)
- **`tree-node-cli`** (`^3.0.0`) : 프로젝트 디렉토리 구조 트리 시각화 CLI 도구
- **`typescript`** (`^5.4.5`) : 정적 타입 체킹 및 ECMAScript 최신 문법 컴파일러
- **`vite`** (`^5.2.11`) : 차세대 초고속 프론트엔드 빌드 툴 및 HMR(Hot Module Replacement) 개발 서버

## 3. 백엔드 API & 데이터 합성·변환 엔진 (`apps/api` - pyproject.toml)
- **`anonymeter>=1.1.0`** : 프라이버시 재식별 위험도(Singling-out, Linkability, Inference) 정량 평가
- **`beautifulsoup4>=4.15.0`** : 비표준 HTML/XML 파싱 및 웹 문서 구조 분석
- **`duckdb>=1.0.0`** : 초고속 인메모리 임베디드 OLAP SQL 분석 엔진 (대용량 데이터 고속 프로파일링)
- **`easyocr>=1.7.0`** : PyTorch 기반 한국어·영어 고정밀 딥러닝 광학 문자 인식(OCR) 엔진
- **`faker>=25.0.0`** : 한국형(ko_KR) 주소, 성명, 주민등록번호, 연락처 등 가명 데이터 생성
- **`fastapi>=0.115.0`** : 고성능 비동기 REST API 웹 프레임워크 및 OpenAPI 3.1 Swagger 자동 생성
- **`html5lib>=1.1`** : HTML5 표준 준수 비정형 HTML 파싱 및 자동 보정
- **`kiwipiepy>=0.20.0`** : 초고속 C++ 한국어 형태소 분석기 및 개인식별정보(PII) 인명·지명·기관명 정밀 추출
- **`lxml>=5.0.0`** : 고성능 C기반 XML/HTML 파서 및 XPath 쿼리 엔진
- **`mammoth>=1.12.0`** : Word(DOCX) 문서를 클린 HTML/Markdown으로 변환
- **`markdown>=3.10.0`** : Python 마크다운 파싱 및 HTML 변환
- **`numpy>=1.26.0`** : 고성능 N차원 수치 배열 및 선형대수 연산
- **`olefile>=0.47`** : 구버전 한글(HWP 5.0) 및 OLE 복합 바이너리 문서 스트림 파싱
- **`opencv-python-headless>=4.9.0`** : 컴퓨터 비전 이미지 처리 및 모폴로지 연산 기반 표(Table) 윤곽선/셀 그리드 검출
- **`openpyxl>=3.1.0`** : Excel(XLSX) 스프레드시트 셀 서식, 다중 시트 읽기/쓰기
- **`pandas>=2.0.0`** : 정형 데이터 테이블 변환, 피벗, 필터링 및 전처리
- **`pdfplumber>=0.11.10`** : PDF 내 테이블 좌표/셀 경계선 정밀 분석 및 텍스트 박스 추출
- **`piexif>=1.1.3`** : 스마트폰 스캔 사진 EXIF 방향 메타데이터 분석 및 자동 회전 정규화
- **`pillow-heif>=1.7.0`** : 아이폰/모바일 촬영 스캔본(HEIC/HEIF) 고화질 무손실 디코딩
- **`pillow>=10.0.0`** : 이미지 포맷 변환, 메타데이터 보존 및 리사이징 (PIL)
- **`playwright>=1.62.0`** : 헤드리스 브라우저 기반 복합 웹 문서 렌더링
- **`pyarrow>=15.0.0`** : Apache Parquet 고성능 대용량 컬럼형 데이터 I/O
- **`pydantic-settings>=2.2.0`** : 환경변수 및 설정 파일 자동 매핑 관리
- **`pydantic>=2.7.0`** : 런타임 데이터 스키마 유효성 검증 및 직렬화
- **`pyhwp>=0.1b15`** : 레거시 HWP 5.0 바이너리 문서 분석, OLE2 복합 포맷 파싱 및 텍스트/테이블 복원
- **`pymupdf>=1.26.0`** : 초고속 PDF 텍스트/레이아웃 추출 및 페이지 렌더링 (fitz)
- **`pypdf>=6.17.0`** : PDF 페이지 분할, 병합 및 메타데이터 처리
- **`python-docx>=1.2.0`** : MS Word(DOCX) XML 구조 파싱 및 스타일 추출
- **`python-hwpx==6.3.0`** : 한국 한글 표준 HWPX(Open 한글) XML 구조 생성, 테이블 조작 및 문서 바인딩
- **`python-multipart>=0.0.9`** : 대용량 스트리밍 파일 업로드/다운로드 처리
- **`python-pptx>=1.0.0`** : 파워포인트(PPTX) 프레젠테이션 슬라이드 파싱, 텍스트·도형·서식 및 이미지 추출/변환
- **`pywin32>=310; sys_platform == 'win32'`** : Windows OS 네이티브 Win32/COM API 바인딩
- **`rapidfuzz>=3.0.0`** : C++ 가속 레벤슈타인 문자열 유사도 분석 및 OCR 문자 오차율(CER) 정밀 측정
- **`rapidocr-onnxruntime>=1.3.0`** : 초고속 PaddleOCR 기반 ONNX 가속 한글/영문 텍스트 및 BBox 인식 엔진
- **`reportlab>=4.1.0`** : PDF 문서 동적 생성, 폰트 임베딩 및 벡터 그래픽 렌더링
- **`scipy>=1.12.0`** : 과학 계산 및 스캔본 문서 미세 기울기(Skew) 자동 보정(Deskewing) 알고리즘
- **`sdv>=1.13.0`** : 다변량 통계 및 딥러닝 기반 합성 데이터 생성 프레임워크 (CTGAN, TVAE, GaussianCopula)
- **`sqlalchemy>=2.0.0`** : ORM 및 SQLite 내장 데이터베이스 트랜잭션 관리
- **`statsmodels>=0.14.0`** : 합성데이터 다변량 통계 가설 검정(KS-test, Chi-square) 및 p-value 정밀 분석
- **`synthetic-engine>=2.1.0`** : 식약처 사내 데이터 생성 엔진 코어 (가명처리, 합성엔진, 프라이버시 평가)
- **`tabulate>=0.10.0`** : 터미널 및 마크다운 텍스트 테이블 자동 포맷팅
- **`torch>=2.0.0`** : PyTorch CPU 최적화 텐서 연산 및 딥러닝 신경망 백엔드
- **`uvicorn[standard]>=0.30.0`** : 고속 비동기 ASGI 웹 서버
- **`weasyprint>=69.0`** : HTML/CSS 기반 표준 인쇄용 PDF 렌더링
- **`xhtml2pdf>=0.2.17`** : HTML+CSS를 PDF로 변환하는 경량 렌더러

## 4. 모노레포 워크스페이스 구조 및 모듈 역할
- **`apps/web`** : React 18 + TypeScript + Vite + Tailwind CSS 기반 고성능 반응형 웹 스튜디오
- **`apps/api`** : FastAPI + SQLite 기반 비동기 백엔드 RESTful API 서버
- **`packages/synthetic_engine`** : 다변량 통계(SDV) 및 프라이버시(Anonymeter) 합성 엔진 코어
- **`packages/contracts`** : 프론트엔드-백엔드 공유 스키마 및 API 계약 인터페이스
