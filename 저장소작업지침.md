# 저장소 작업 지침

## 프로젝트 구조와 모듈 구성

이 저장소는 합성데이터 스튜디오를 위한 모노레포다. React/Vite 프런트엔드는 `apps/web`에 있으며 화면 코드는 `apps/web/src`, 정적 자원은 `apps/web/public`에 둔다. FastAPI 백엔드 패키지는 `apps/api/src/synthetic_api`, 백엔드 테스트는 `apps/api/tests`에 둔다. 핵심 합성데이터 로직은 `packages/synthetic_engine`으로 분리하고 테스트는 `packages/synthetic_engine/tests`에 둔다. 공통 API 계약과 스키마는 `packages/contracts`에서 관리한다. 실행 파일과 생성 산출물은 `apps/storage` 아래에 두며, 명시적으로 필요한 경우가 아니면 생성물을 커밋하지 않는다.

## 빌드·테스트·개발 명령

- `npm run dev`: `apps/web` 워크스페이스로 웹 앱을 실행한다.
- `npm run build`: TypeScript 검사와 웹 앱 빌드를 실행한다.
- `npm run build --workspace=apps/web`: 프런트엔드 범위만 빌드한다.
- `npm run tree:cli`: 큰 폴더를 제외한 프로젝트 트리를 출력한다.
- `uv run pytest`: uv 환경이 준비된 경우 전체 Python 테스트를 실행한다.
- `uv run pytest apps/api/tests packages/synthetic_engine/tests`: 백엔드와 엔진 테스트를 명시적으로 실행한다.

Windows에서는 `C:\식약처\work`에서 명령을 실행한다. WSL에서 선택적 네이티브 패키지를 사용할 수 없으면 프로젝트가 이미 사용하는 Windows Node/Python 환경을 우선한다.

## 코딩 스타일과 이름 규칙

프런트엔드는 TypeScript, React 함수형 컴포넌트, Tailwind 유틸리티 클래스, `lucide-react` 아이콘을 사용한다. React 컴포넌트는 `PascalCase`, 훅은 `useSomething`으로 이름을 짓고 공통 UI는 `apps/web/src/components`에 둔다. 새 스타일을 만들기 전에 `ui-panel`, `ui-button-primary`, `ui-button-secondary`, `ui-label`, `ui-section-title` 같은 기존 클래스를 우선 사용한다. Python은 3.10 이상을 대상으로 하며 `src/synthetic_api` 또는 `synthetic_engine` 패키지를 사용한다. 오케스트레이션, 도메인 로직, 인프라 책임은 분리한다.

## 테스트 지침

Python 테스트는 `pyproject.toml` 설정에 따라 `test_*.py` 이름을 사용한다. 합성 작업, 일괄 처리, 파일 파싱, 개인정보 보호 로직처럼 변경 영향이 큰 영역에는 동작 중심의 테스트를 추가한다. 프런트엔드 변경 후에는 현재 TypeScript 검증 경로인 `npm run build --workspace=apps/web`를 항상 실행한다.

## 커밋과 풀 리퀘스트 지침

최근 커밋은 `feat:`, `style:` 같은 Conventional Commit 접두어와 간결한 한국어 설명을 사용한다. 커밋은 하나의 논리적 변경으로 범위를 제한한다. 풀 리퀘스트에는 사용자에게 보이는 변경, 검증 명령, 영향 영역(`apps/web`, `apps/api` 등)을 적고 의미 있는 UI 변경에는 화면 캡처를 첨부한다.

## 에이전트 전용 지침

이 저장소 안의 파일만 수정한다. 데이터 변환 영역은 Antigravity 담당이므로 명시적으로 요청하지 않는 한 변환 동작을 바꾸지 않는다. 작업 흐름, 업로드, 카드, 버튼, 섹션 제목을 새로 복제하기보다 공통 UI 컴포넌트와 백엔드 오케스트레이션 도우미를 우선 사용한다.
